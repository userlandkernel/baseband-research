/* 
 * This is an automatically-generated file.
 * Source file REMOVED_FOR_TESTING
 * Output file  : -
 * Platform     : V4 Generic
 * Mapping      : CORBA C
 *
 * Generated by Magpie VERSION_REMOVED
*/

#define IDL4_OMIT_FRAME_POINTER 0 /* Unimplemented */
#define IDL4_USE_FASTCALL 0 /* Unimplemented */
#define IDL4_NEED_MALLOC 0 /* Unimplemented */
#define IDL4_API v4
#define IDL4_ARCH generic




/* NOTE: Change the following line to correctly include the service stubs. */
#include "test/input/const_propagation_service.h"






IDL4_INLINE long keypad_interface_device_create_implementation(CORBA_Object _caller, long driver, idl4_server_environment *_env)
{
	long retval;
	
	/* Implementation of keypad_interface_device::create */
	return retval;
}

/* Link the name of the function above with the name defined in the default vtable. */
IDL4_PUBLISH_KEYPAD_INTERFACE_DEVICE_CREATE(keypad_interface_device_create_implementation);



/* Use the default dispatch table defined in the service header. */
void * keypad_interface_device_vtable[KEYPAD_INTERFACE_DEVICE_DEFAULT_VTABLE_SIZE]
		= KEYPAD_INTERFACE_DEVICE_DEFAULT_VTABLE;





IDL4_INLINE long keypad_interface_device2_create_implementation(CORBA_Object _caller, long driver, idl4_server_environment *_env)
{
	long retval;
	
	/* Implementation of keypad_interface_device2::create */
	return retval;
}

/* Link the name of the function above with the name defined in the default vtable. */
IDL4_PUBLISH_KEYPAD_INTERFACE_DEVICE2_CREATE(keypad_interface_device2_create_implementation);



/* Use the default dispatch table defined in the service header. */
void * keypad_interface_device2_vtable[KEYPAD_INTERFACE_DEVICE2_DEFAULT_VTABLE_SIZE]
		= KEYPAD_INTERFACE_DEVICE2_DEFAULT_VTABLE;

void server(void)
{
	L4_ThreadId_t partner;
	L4_MsgTag_t msgtag;
	idl4_msgbuf_t msgbuf;
	long cnt;

	while (1) {
		partner = L4_nilthread; /* Our initial reply is to the nilthread. */
		msgtag.raw = 0;
		cnt = 0;

		while (1) {
			idl4_reply_and_wait(&partner, &msgtag, &msgbuf, &cnt);
			if (idl4_is_error(&msgtag)) {
				/* FIXME: Add your error handler here. */
				printf("server: error sending IPC reply\n");
				break; /* Reset thread ID to nilthread & try again */
			}
			switch(magpie_get_interface_id(&msgbuf)) {
				
				case KEYPAD_UUID_DEVICE:
					idl4_process_request(&partner, &msgtag, &msgbuf, &cnt,
							keypad_interface_device_vtable
							[idl4_get_function_id(&msgtag) & KEYPAD_INTERFACE_DEVICE_FID_MASK]);
					break;
				
				case KEYPAD_UUID_DEVICE2:
					idl4_process_request(&partner, &msgtag, &msgbuf, &cnt,
							keypad_interface_device2_vtable
							[idl4_get_function_id(&msgtag) & KEYPAD_INTERFACE_DEVICE2_FID_MASK]);
					break;
				
			}
		}
	}
}


